apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: lab-app
  labels:
    app: nginx
    app.kubernetes.io/name: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
